{
  "buildCommand": "npm install -g pnpm && cd yours-product && pnpm install --frozen-lockfile && pnpm run build:vercel",
  "outputDirectory": "yours-product/dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
